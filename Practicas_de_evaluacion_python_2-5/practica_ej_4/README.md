# Ejercicio 4

Esta práctica contiene una suite de tests que comprueba las tres funciones desarrolladas en los ejercicios anteriores:

- `esBinario(strbinario)` del ejercicio 2
- `estaEnRango(valor, minimo, maximo)` del ejercicio 3
- `estaEnLista(valor, lista)` del ejercicio 3

## Ejecutar los tests

Desde el directorio raíz, ejecutamos:

```bash
python -m practica_ej_4.app.main
